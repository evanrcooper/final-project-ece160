In this directory is just a proof of concept for tcp communication.

Python Structs are packed and sent from the Server running on localhost, 12345, and the client recieves a string representing the color of the card, and the number of the card
